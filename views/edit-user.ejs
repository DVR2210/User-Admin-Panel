<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit User - Admin Panel</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <h1>Edit User</h1>
  <form action="/users/edit/<%= user.id %>" method="POST">
    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
    <div><input type="text" name="username" value="<%= user.username %>" required></div>
    <div><input type="password" name="password" placeholder="Leave blank to keep current"></div>
    <div><input type="text" name="first_name" value="<%= user.first_name %>" required></div>
    <div><input type="text" name="last_name" value="<%= user.last_name %>" required></div>
    <div>
      <select name="gender" required>
        <option value="male" <%= user.gender === 'male' ? 'selected' : '' %>>Male</option>
        <option value="female" <%= user.gender === 'female' ? 'selected' : '' %>>Female</option>
        <option value="other" <%= user.gender === 'other' ? 'selected' : '' %>>Other</option>
      </select>
    </div>
    <div><input type="date" name="birthdate" value="<%= user.birthdate %>" required></div>
    <button type="submit">Update User</button>
  </form>
  <a href="/users">Back to Users</a>
</body>
</html>